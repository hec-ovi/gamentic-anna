# local env + databases (never publish)
.env
*.db
*.db-*

# secrets (PyPI token for publishing the executa, etc.) - NEVER commit
.secrets/

# anna cli working dir (dev tokens, recordings, staged bundle)
.anna/

# deps / build / caches
node_modules/
dist/
.venv/
__pycache__/
.pytest_cache/

# os
.DS_Store
Thumbs.db
session
