# local env + databases (never publish)
.env
*.db
*.db-*

# anna cli working dir (dev tokens, recordings, staged bundle)
.anna/

# deps / build / caches
node_modules/
dist/
.venv/
__pycache__/
.pytest_cache/

# os
.DS_Store
Thumbs.db
