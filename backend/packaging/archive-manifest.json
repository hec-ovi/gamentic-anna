{
  "_comment": "Ships at the ROOT of the per-platform tar.gz. Anna's binary installer reads it to pin the entrypoint for a --onedir bundle (otherwise it fuzzy-guesses the wrong file). Identity is the server-minted tool_id, not this name.",
  "name": "gamentic-executa",
  "version": "0.2.0",
  "runtime": {
    "binary": {
      "entrypoint": "gamentic-executa"
    }
  }
}
